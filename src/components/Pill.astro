---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span
  class="inline-flex items-center rounded-full border border-stone-300 px-3 py-1 text-[0.7rem] font-semibold uppercase tracking-[0.18em] text-stone-700"
>
  {label}
</span>
