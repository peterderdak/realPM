---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span
  class="inline-flex items-center rounded-full border border-black/8 bg-white/88 px-3 py-1.5 text-[0.68rem] font-semibold uppercase tracking-[0.24em] text-zinc-700"
>
  {label}
</span>
