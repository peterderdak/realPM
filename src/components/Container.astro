---
interface Props {
  as?: string;
  class?: string;
}

const { as: Tag = "div", class: className } = Astro.props;
---

<Tag class:list={["mx-auto w-full max-w-[1180px] px-5 sm:px-6 lg:px-8", className]}>
  <slot />
</Tag>
